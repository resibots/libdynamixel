#!/usr/bin/env python
# encoding: utf-8


def build(bld):
    bld(features='cxx cxxprogram', source='test.cpp', target="dynamixel_test", includes=". ..")
    bld(features='cxx cxxprogram', source='omnipointer_arm.cpp', target="omnipointer_arm", includes=". ..")
